interface:
  display_name: "Build"
  short_description: "Build tickets, review, and verify"
  default_prompt: "Use $build to implement these tickets, review the complete diff, and verify the result."
